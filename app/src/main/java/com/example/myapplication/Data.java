package com.example.myapplication;

import org.tensorflow.lite.Interpreter;

import java.nio.MappedByteBuffer;
import java.util.List;
import java.util.Map;

public class Data {
    private Object assetManager;
   /*  데이터가 완성 됬을때 시도
   MappedByteBuffer tfliteModel = loadModelFile(assetManager, MODEL_PATH);
    Interpreter tflite = new Interpreter(tfliteModel);// <--- 가져올 파일
    Map<String, Integer> wordIndexMap = loadWordIndexMap(assetManager, WORD_INDEX_PATH);

    String inputText = "시험 문제";
    List<String> tokens = tokenize(inputText);*/




}
